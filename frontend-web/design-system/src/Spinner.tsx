/**
 * The brand loading spinner — a circular indeterminate loader in the accent
 * color, centered with vertical margin. Takes no props.
 * @category Feedback
 */
export function Spinner() {
  return <div className="spinner" />;
}
